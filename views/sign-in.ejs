<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>sign-in</title>
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/login-form.css">
</head>
<body>
    <% if (membErr) { %>
        <div class="memb-err">회원만 게시글을 볼 수 있습니다!</div>
    <% } %>
    <form action="" method="post">
        <h3>로그인</h3>
        <label for="username">사용자 이름: </label>
        <input type="text" name="username" id="username" required>
        
        <% if (message.length > 0 && message[0] === "Incorrect username" ) { %>
            <div class="auth-err">존재하지 않는 사용자입니다.</div>
        <% } %>

        <label for="password">비밀번호: </label>
        <input type="password" name="password" id="password" required>

        <% if (message.length > 0 && message[0] === "Incorrect password" ) { %>
            <div class="auth-err">비밀번호가 올바르지 않습니다.</div>
        <% } %>

        <button type="submit" class="confirm-btn">확인</button>
        <a href="/" class="cancel-btn">취소</a>
        <% if (membErr) { %>
            <a href="/sign-up" class="sign-up-link">회원가입</a>
        <% } %>
    </form>
</body>
</html>